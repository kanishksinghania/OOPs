#include<iostream>
using namespace std;

class Solution{
    public:
    void accept() {
        int a,b[5],c;
        cout<<"Enter the number of elements: ";
        cin>>a;
        cout<<"Enter the values:";
        for(int i=0;i<a;i++) {
            cin>>b[i];
        }
        cout<<"Enter the target number: ";

    }
    void result(){
        if(b)
    }

};